// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model AccessPoint {
  id Int @id @default(autoincrement())
  networks Network[]
  description String
  coordinate Coordinate @relation(fields: [coordinateId], references: [id])
  coordinateId Int @unique
}

model Network {
  bssid String @unique
  ssid String
  fingerprints FingerprintDetail[]
  ap AccessPoint @relation(fields: [apId], references: [id])
  apId Int
}

model Fingerprint {
  id Int @id @default(autoincrement())
  createdAt DateTime @default(now())
  fingerprintDetails FingerprintDetail[]
  location String
}

model FingerprintDetail {
  id Int @id @default(autoincrement())
  fingerprint Fingerprint @relation(fields: [fingerprintId], references: [id])
  fingerprintId Int @unique
  network Network @relation(fields: [bssid], references: [bssid] )
  bssid String
  rssi Float
}

model Floor {
  id Int @id @default(autoincrement())
  name String
  level Int @unique
  rooms Room[]
  corridors Corridor[]
}

model Room {
  id Int @id @default(autoincrement())
  name String
  floor Floor @relation(fields: [floorId], references: [id])
  floorId Int 
  coordinates Coordinate[] @relation(name: "roomCoordinateOf")
  poiX Float
  poiY Float
}

model Corridor {
  id Int @id @default(autoincrement())
  name String
  floor Floor @relation(fields: [floorId], references: [id])
  floorId Int 
  coordinates Coordinate[] @relation(name: "corridorCoordinateOf")
  poiX Float
  poiY Float
}

model Coordinate {
  id Int @id @default(autoincrement())
  x Float
  y Float
  room Room? @relation(fields: [roomId], references: [id], name: "roomCoordinateOf")
  roomId Int?
  corridor Corridor? @relation(fields: [corridorId], references: [id], name: "corridorCoordinateOf")
  corridorId Int?
  ap AccessPoint?
}