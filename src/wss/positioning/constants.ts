const threshold = 0.5;
const baseRssi = 20 // mwatts
const trilaterationScaleFactor = 1e12;
const speedOfLight = 3e8
const pathLossExponent = 1.5
const freq = 2.4e9
const fourPi = 4 * Math.PI

export { threshold, baseRssi, trilaterationScaleFactor, speedOfLight, pathLossExponent, freq, fourPi};
